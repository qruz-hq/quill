<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- whisper ships its own dylibs, which are not signed by the same team -->
  <key>com.apple.security.cs.disable-library-validation</key><true/>
  <!-- Hardened runtime strips DYLD_* by default. The whisper binaries now use
       @loader_path so this is not required, but it keeps the dev path working. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key><true/>
  <!-- the Swift helper is spawned as a child process -->
  <key>com.apple.security.cs.allow-jit</key><true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
  <key>com.apple.security.device.audio-input</key><true/>
</dict>
</plist>
